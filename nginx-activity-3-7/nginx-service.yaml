apiVersion: v1
kind: Service
metadata:
  name: faizal-nginx-service
  namespace: mfaizalbe-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80