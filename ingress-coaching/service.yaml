apiVersion: v1
kind: Service
metadata:
  name: faizal-echo-service
  namespace: faizal-eks-activity
spec:
  selector:
    app: echo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678